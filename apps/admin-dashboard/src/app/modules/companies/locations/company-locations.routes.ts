import { Route } from '@angular/router';
import { CompanyLocationsComponent } from 'app/modules/companies/locations/company-locations.component';

export const companyLocationsRoutes: Route[] = [
    {
        path: '',
        component: CompanyLocationsComponent,
    },
];
